
 <div> 
{{#each imagesD.images}}
<div style="">
    <img src="data:image/png;base64,{{this.imageData}}" class="h-auto max-w-full rounded-lg" alt="image description"/>
    <div style="">
    <p class="mt-2 text-sm text-center text-gray-500 dark:text-gray-400">Prompt: {{this.prompt}}</p >
    </div>
   <form action="/knifedrive/download-image" method="POST">
    <input type="hidden" name="imageUrl" value="{{this.id}}" />
    <button type="submit">download</button>
  </form>
  <form class="mt-2 text-sm text-center text-gray-500 dark:text-gray-400" action="/knifedrive/erase/{{this.id}}" method="POST">
    <button type="submit">Delete</button>
  </form>
 
{{/each}}

</div>
